import {Metadata} from 'next'
import {Toaster} from "react-hot-toast"
import {ThemeProvider} from "@/components/theme-provider"
import {Header} from "@/components/Header"

import "./globals.css"
import NextTopLoader from "nextjs-toploader";

export const metadata: Metadata = {
    title: {
        default: "Familia",
        template: "%s | Familia"
    },
    description: "Familia is a comprehensive family tree application",
    keywords: ["family tree", "genealogy", "SaaS", "familia"],
    authors: [{name: "Your Name"}],
    creator: "Your Company Name",
    // themeColor: [
    //     { media: "(prefers-color-scheme: light)", color: "white" },
    //     { media: "(prefers-color-scheme: dark)", color: "black" }
    // ],
    openGraph: {
        type: "website",
        locale: "en_US",
        url: "https://your-domain.com",
        siteName: "Familia",
        title: "Familia - Your Family Tree Solution",
        description: "Discover and manage your family history with Familia",
        images: [
            {
                url: "https://your-domain.com/og-image.jpg",
                width: 1200,
                height: 630,
                alt: "Familia Open Graph Image",
            },
        ],
    },
    twitter: {
        card: "summary_large_image",
        title: "Familia - Your Family Tree Solution",
        description: "Discover and manage your family history with Familia",
        images: ["https://your-domain.com/twitter-image.jpg"],
        creator: "@yourtwitter",
    },
    icons: {
        icon: "/favicon.ico",
        shortcut: "/favicon-16x16.png",
        apple: "/apple-touch-icon.png",
    },
    // manifest: "/site.webmanifest",
}

interface RootLayoutProps {
    children: React.ReactNode
}

export default function RootLayout({children}: RootLayoutProps) {
    return (
        <html lang="en" suppressHydrationWarning>
        <body className="min-h-screen bg-background font-sans antialiased">
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
            {/*<GlobalLoadingIndicator />*/}
            <div className="relative flex min-h-screen flex-col">
                <Header/>
                <NextTopLoader
                    color={"#006400"}
                />
                <main className="flex-1">
                    <div className="container mx-auto py-8">
                        {children}
                    </div>
                </main>
                <footer className="border-t">
                    <div className="container mx-auto py-4 text-center text-sm text-muted-foreground">
                        © {new Date().getFullYear()} Familia. All rights reserved.
                    </div>
                </footer>
            </div>
            <Toaster/>
        </ThemeProvider>
        </body>
        </html>
    )
}

